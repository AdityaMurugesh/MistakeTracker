<?xml version="1.0" encoding="utf-8"?>
<!--
  Allow cleartext HTTP only to the local Ollama server reached via the
  Android emulator's host-loopback alias (10.0.2.2) or a typical LAN
  range. The rest of the app stays cleartext-blocked.

  Why we need this: Android 9+ blocks cleartext HTTP by default. Ollama
  serves over plain HTTP on :11434 with no easy TLS option, so we
  whitelist just the addresses a developer would reasonably point at.
-->
<network-security-config>
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">10.0.2.2</domain>
    <domain includeSubdomains="false">localhost</domain>
    <domain includeSubdomains="false">127.0.0.1</domain>
  </domain-config>
</network-security-config>
