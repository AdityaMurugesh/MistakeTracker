// Owner: Capture
// Widget test for the most important UX surface.
//
// TODO:
//   - Pump EntryForm with no `existing` -> renders empty form
//   - Fill `what` field, tap save -> EntryDao.insert called once
//   - Empty `what` -> save disabled (or error toast)

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('EntryForm', () {
    test('placeholder — replace with widget tests once form is built', () {
      expect(true, isTrue);
    });
  });
}
